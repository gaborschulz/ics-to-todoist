[tool.poetry]
name = "ics-to-todoist-v2"
version = "0.1.0"
description = ""
authors = ["Gabor Schulz <gabor@gaborschulz.com>"]
readme = "README.md"
packages = [{ include = "ics_to_todoist_v2" }]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.bandit]